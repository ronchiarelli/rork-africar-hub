{
  "name": "gocarhub",
  "version": "1.0.0",
  "private": true,
  "description": "GoCar Hub — Supabase project (schema, migrations, Edge Functions) for the expo/ app",
  "scripts": {
    "supabase": "supabase",
    "db:diff": "supabase db diff",
    "db:push": "supabase db push",
    "functions:deploy": "supabase functions deploy"
  },
  "devDependencies": {
    "supabase": "^2.109.0"
  }
}
